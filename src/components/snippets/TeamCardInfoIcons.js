import React from 'react';
import styled, { withTheme } from 'styled-components' 

const TeamCardInfoIcons = (props) => {

  return (
    <ul className="TeamCardInfoIcons">
    </ul>
  );
}

export default withTheme(TeamCardInfoIcons);